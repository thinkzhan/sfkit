#!/usr/bin/env node

var program = require('commander');
var pkg = require('../package.json');

program.version(pkg.version)
	.option('--init', 'single-js|velocity-mutil-page|mdev-mutil-page')
	.parse(process.argv);

require('../index').init({});
